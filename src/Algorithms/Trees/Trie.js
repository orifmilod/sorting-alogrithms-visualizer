const Node = {
  value: '',
  children: new Array(26),
  isComplete: false 
};

const ParentNode = {
 
}
   
export  function Trie() {
  this.head = {

  }

  Insert = () => {
    
  }
  
}

Trie.prototype.Insert = function(str) {
  var curNode = this.head;
  var newNode = null;
  var curChar = str.slice(0,1);
  
}